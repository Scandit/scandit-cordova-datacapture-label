{
  "name": "scandit-cordova-datacapture-label",
  "version": "8.2.1",
  "description": "Scandit Data Capture SDK for Cordova",
  "license": "Apache-2.0",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/dts/index.d.ts",
  "keywords": [
    "ecosystem:cordova",
    "cordova-ios",
    "cordova-android"
  ],
  "scripts": {
    "build": "rollup -c rollup.config.js --bundleConfigAsCjs",
    "test": "npm run lint",
    "lint": "eslint ./www/ts",
    "coverage": "jest --coverage --maxWorkers=12",
    "clear-jest-cache": "jest --clearCache || true",
    "clean": "npm run clear-jest-cache && rimraf dist coverage",
    "clean-all": "npm run clean && rimraf node_modules"
  },
  "dependencies": {
    "scandit-cordova-datacapture-barcode": "8.2.1",
    "scandit-cordova-datacapture-core": "8.2.1",
    "scandit-datacapture-frameworks-label": "8.2.1"
  },
  "cordova": {
    "id": "scandit-cordova-datacapture-label",
    "platforms": [
      "ios",
      "android"
    ]
  }
}
